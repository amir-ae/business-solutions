@page
@model CreateProviderModel
@{
    ViewBag.Workflow = "CreateProvider";
}
@if (TempData.ContainsKey("message"))
{
    <div class="alert alert-success">@TempData["message"]</div>
}
<form method="post">
    <div class="form-group my-2">
        <label asp-for="Provider.ProviderName"></label>
        <div>
            <span asp-validation-for="Provider.ProviderName" class="text-warning"></span>
        </div>
        <input asp-for="Provider.ProviderName" class="form-control" />
    </div>
    <div class="mt-3">
        <button type="submit" class="btn btn-success">Create</button>
        <a asp-page="CreateProvider" class="btn btn-secondary">Cancel</a>
    </div>
</form>




